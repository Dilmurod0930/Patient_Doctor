import 'package:flutter/material.dart';

class HospitlPage extends StatelessWidget {
  const HospitlPage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}